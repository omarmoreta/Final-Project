import Phaser from "phaser";

export default class LoginScene extends Phaser.Scene {
    constructor() {
        super('Login');
    }
    preload() {
    }
    create() {
    }
    update() {
    }
}
